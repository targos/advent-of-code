// https://adventofcode.com/2022/day/13

import { readInput } from '../utils.js';

for await (const line of readInput(import.meta.url, splitInput)) {
  console.log(line);
}

function splitInput(input) {
  return input;
}
